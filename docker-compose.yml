version: '3.8'

services:
  bot:
    build: .
    container_name: telegram-rich-markdown-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
